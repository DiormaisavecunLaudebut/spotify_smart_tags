var iconCheck = document.querySelector('.option-checked');
var checkBox = document.getElementById('filter-all-checkbox');



function styleCheckbox() {
  if (window.location.href.match('account')) {
    iconCheck.classList.toggle('font0')
    checkBox.classList.toggle('opacity0')
  }
}

styleCheckbox();
